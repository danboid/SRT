# SRT
Snapper BTRFS snapshot restore tool

It features an easy to use text user interface (based upon dialog) to select the snapshot you wish to restore with rsync.

SRT also uses xmlstarlet to parse Snapper's snapshot info.xml files to build the snapshot selection menu.
